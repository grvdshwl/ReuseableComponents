import Throttle from "../../Components/Throttle";
import { CommonTitle, CommonWrapper } from "../AppUI/App.styles";

const ThrottleUI = () => {
  return (
    <CommonWrapper>
      <CommonTitle>Throttle</CommonTitle>
      <Throttle />
    </CommonWrapper>
  );
};

export default ThrottleUI;
