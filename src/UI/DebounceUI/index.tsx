import Debounce from "../../Components/Debounce";
import { CommonTitle, CommonWrapper } from "../AppUI/App.styles";

const DebounceUI = () => {
  return (
    <CommonWrapper>
      <CommonTitle>Debounce</CommonTitle>
      <Debounce />
    </CommonWrapper>
  );
};

export default DebounceUI;
